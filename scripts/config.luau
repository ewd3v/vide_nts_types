return {
	analyzePaths = { "src", "tests", "scripts" }, -- Paths that should have their .luau files analyzed / linted / formatting checked
	buildRuntimes = { "roblox" }, -- Runtimes to build and publish for
	testRuntimes = {}, -- Runtimes to run tests on
}
